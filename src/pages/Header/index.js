import { NavLink } from 'react-router-dom';

const Header = () => {
  return (
    <div style={{ height: '100%' }}>
      <NavLink to="/sd_cards">Header</NavLink>
    </div>
  );
};

export default Header;
